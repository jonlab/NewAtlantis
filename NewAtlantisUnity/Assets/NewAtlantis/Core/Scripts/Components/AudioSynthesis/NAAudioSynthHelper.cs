using UnityEngine;
using System.Collections;

public class NAAudioSynthHelper
{
	/*
	public static float[] GetOrCreateAudioBuffer(GameObject go, float duration, int samplerate)
	{
		AudioSource audio = go.GetComponent<AudioSource>();
		if (audio)
		{
			int samplecount = (int)(samplerate*duration);
			audio.clip = AudioClip.Create("NAAudioSynthNoise", samplecount, 1, samplerate, false);

		}
		else
		{
			Debug.LogWarning ("Error, you need to add an AudioSource to your object.");
			return null;
		}
	}
	*/
}
